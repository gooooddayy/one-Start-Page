# One起始页 Chrome 扩展安装指南

## 已完成的配置

✅ 扩展已打包为 CRX3 格式（含公钥，扩展 ID 固定）
✅ Chrome 注册表策略已设置（ExtensionInstallAllowlist + ExtensionInstallSources + DeveloperToolsAvailability）
✅ Edge 注册表策略已设置（同上）
✅ 扩展已复制到永久路径：C:\Users\Administrator\AppData\Local\OneStartPage

扩展 ID：hkmgioekbbgeppoggomajdbcnpknjhid

---

## 安装方式

### 方式一：加载已解压扩展（最可靠，强烈推荐）

1. 打开 Chrome，地址栏输入 `chrome://extensions/`
   - Edge 用户输入 `edge://extensions/`
2. 右上角打开「开发者模式」开关
3. 点击左上角「加载已解压的扩展程序」
4. 选择文件夹：`C:\Users\Administrator\AppData\Local\OneStartPage`
5. 安装完成！打开新标签页即可看到起始页

### 方式二：拖拽 CRX 安装

1. 打开 `chrome://extensions/`（Edge: `edge://extensions/`）
2. 开启「开发者模式」
3. 将桌面上的 `one起始页.crx` 文件拖入扩展页面
4. 在弹窗中点击「添加扩展程序」

### 方式三：通过 ZIP 安装

1. 解压桌面上的 `one起始页.zip`
2. 打开 `chrome://extensions/`，开启「开发者模式」
3. 点击「加载已解压的扩展程序」，选择解压后的文件夹

---

## 已设置的注册表策略

### Chrome (HKLM\SOFTWARE\Policies\Google\Chrome)
- ExtensionInstallAllowlist[1] = hkmgioekbbgeppoggomajdbcnpknjhid
- ExtensionInstallSources[1] = file:///*
- ExtensionInstallSources[2] = https://*/*
- ExtensionSettings[hkmgioekbbgeppoggomajdbcnpknjhid] = {"installation_mode":"allowed"}
- DeveloperToolsAvailability = 1

### Edge (HKLM\SOFTWARE\Policies\Microsoft\Edge)
- 同上

---

## 如需卸载

1. 打开 `chrome://extensions/`
2. 找到「One起始页」扩展，点击「移除」
3. 如需清除注册表策略，删除以下注册表项：
   - HKLM\SOFTWARE\Policies\Google\Chrome\ExtensionInstallAllowlist
   - HKLM\SOFTWARE\Policies\Google\Chrome\ExtensionInstallSources
   - HKLM\SOFTWARE\Policies\Google\Chrome\ExtensionSettings
   - HKLM\SOFTWARE\Policies\Microsoft\Edge\ExtensionInstallAllowlist
   - HKLM\SOFTWARE\Policies\Microsoft\Edge\ExtensionInstallSources
   - HKLM\SOFTWARE\Policies\Microsoft\Edge\ExtensionSettings
